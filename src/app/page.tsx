import Home from '@/components/home';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Inicio | La Voz de OIENIV',
  description: 'Radio Cristiana en Venezuela',
};

export default Home;
